// Jenkinsfile (Declarative Pipeline)
pipeline {
  agent any
  stages {
    stage('Stage 1') {
      steps {
        sh "podman ps"
        sh "podman build --tag mytest:test -f ./Dockerfile"
        sh "podman images"
      }
    }
  }
}